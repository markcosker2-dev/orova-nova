---
name: session-2026-07-29-handoff
description: Cold-start handoff — 7 PRs shipped, a live cold-email incident, corrections to long-held false beliefs, and the exact next code task
type: session
created: 2026-07-29
status: active
---

# OROVA NOVA — Handoff (2026-07-29)

## 0. Read first
`CLAUDE.md` → [[active-context]] → this file → [[0014-licence-registries-as-the-discovery-source|ADR-0014]]
→ [[audit-2026-07-26-phase5-final]]. Previous handoff: [[session-2026-07-24-handoff]]
(**contains a factual error — see §2**).

## 1. Production state
- **main `fc3bfdd`**; `/health` build `fc3bfddf706c`, `db: ok`, `memory: ok`. Verified.
- **ONE PR OPEN AND UNMERGED: [#118] — `chore/system-audit-2026-07-26`, MERGEABLE.**
  It carries the audit + every Phase 2/3 fix + the outreach safety gates.
  **Merging it is the first thing to do.** 628 tests pass on that branch.
- Local branch is `chore/system-audit-2026-07-26`, clean, pushed.

## 2. 🔴 CORRECTIONS — prior handoffs were WRONG about this
**"Zero emails have ever been sent. Zero prospect conversations."** That claim is in
[[session-2026-07-24-handoff]], [[active-context]] and both audit reports. **It is false.**

Production metrics say **`emails_sent = 48`, `replies_received = 0`.** It came from a
stale handoff and was never checked against `/api/metrics`. Do not repeat it.

Two other corrections made this session, both after checking the live system rather
than a document:
- **Retell CAN book meetings.** `check_availability_cal` + `book_appointment_cal` are
  configured with a real `event_type_id 2804866`. An earlier claim of "no booking
  tool" came from reading the MCP wrapper's response, which **omits `general_tools`**.
  Always verify Retell against the REST API, not the MCP tool.
- **CAN-SPAM logic was already live** in `agentmail_skill._apply_compliance_footer`.
  `app/core/compliance.py` is a redundant second implementation and was deliberately
  NOT wired in.

**Lesson worth keeping: verify against the running system, not the vault.**

## 3. 🚨 THE LIVE INCIDENT (2026-07-25/26)
The outreach lane worked through **51 stored rows all tagged `vertical: "Automotive"`**
and sent **48 cold emails**. Recipients included:

- `google.com` via a probe address `asxvmprobertest@gmail.com`
- `museo@adolfoalsina.gov.ar` — an Argentine **government museum**
- `autotrader.com`, `autonews.com`, `automotiveworld.com` — marketplaces/trade press
- `name@hotmail.com` — a placeholder address

Result: **0 replies**, `550 5.4.1 Access denied` bounces from Microsoft, and sender
reputation spent on a segment ADR-0012 had already disqualified.

**Three independent failures, all fixed in #118 (not yet deployed):**
1. ADR-0012 says "disqualify on sight: general auto repair, franchised dealers" — but
   that lived only in a document. **A strategy decision that is not encoded is not a
   control.** Now in `lead_validator.off_icp_vertical_reason`.
2. The CAN-SPAM footer **warned and sent anyway** when `BUSINESS_POSTAL_ADDRESS` was
   unset, so all 48 shipped without the address §7704 requires. Now fail-closed.
3. Nothing checked the ICP at send time. Pre-send gate added, fails closed.

**Mark has set `OUTREACH_AUTOPILOT=0` on Render — sends are stopped.** Verified: no
sends in the log window. Leave it at 0 until #118 is deployed.

## 4. What shipped (merged to main, deployed)
| PR | What |
|---|---|
| #109 | Ad-signal detector + `ad_tier` — Meta Pixel / marketplace badges from homepage HTML. Live-validated on 9 real CA remodelers: 2 hot / 2 warm / 5 cold |
| #110 | `lead.state` persisted — without it the CA/WA/OR registry router got `""` and fell to dead OpenCorporates |
| #112 | SerpAPI pagination + fallback demotion — it was slicing `[:count]` BEFORE the website filter, discarding paid-for results |
| #113 | WA L&I owner registry replacing the anti-bot-gated WA SoS endpoint |
| #114 | Recovery: #110/#112 had merged into intermediate branches, not main |
| #115 | ADR-0014 + session note |
| #116, #117 | Opt-out → DNC (TCPA); off-ICP domain quarantine (6 rows cleared live) |

**Also live in Retell (config, not code):** outbound prompt now USES the booking tools
(it previously said "there is no booking link" and instructed manual capture —
actively suppressing bookings); inbound agent given the same Cal tools; voicemail
fixed (`enable_voicemail_detection` is a DEAD field — Retell migrated to
`voicemail_option`); `boosted_keywords` updated from `["Automotive"]`.

## 5. Hard constraints (unchanged)
- **Mark is in the Philippines (UTC+8)**, targeting US. Retell owns ALL cold dialling.
- **$0 budget.** No domain, **no business email — personal Gmail only.**
- **512MB Render free tier.** `httpx==0.27.2` pinned. Deploys wipe SQLite; Drive restores.
- **Never fabricate.** No invented leads, case studies, or portfolio work.
- Commit trailer: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
- **Merge trap:** GitHub only auto-retargets a stacked PR when its base branch is
  DELETED. Merging a stack with `--delete-branch=false` silently lands children on
  intermediate branches. **Always grep the merged code on main — never trust a row of
  `MERGED` statuses.**

## 6. 🔴 THE ACTUAL BLOCKER
**SerpAPI is exhausted: 250/250, 0 searches left** (verified against SerpAPI's own
account API). A live Seattle hunt returned **0 leads**. No automated discovery until
the monthly reset. No reset date is exposed by their API — assume the 1st.

Every commercial alternative was tested live and is closed at $0: Meta Ad Library API
(EU/UK-only for commercial ads) · Apollo free (`API_INACCESSIBLE` **plan-wide**, 0
export credits) · Composio→Facebook (Pages-only, search Error #10) · OpenCorporates
(£2,250/yr) · Yelp Fusion (ambiguous free tier + approval wait).

## 7. ▶ NEXT CODE TASK — WA licence-board discovery (ADR-0014 seam 1)
The unblock, and it is free and unlimited. **Verified live:**

| Source | Endpoint | Volume | Owner name | Phone |
|---|---|---|---|---|
| WA L&I | `https://data.wa.gov/resource/m8qx-ubtq.json` (Socrata, **no key**) | 75,515 ACTIVE | **100%** | **100%** |
| OR CCB | `https://data.oregon.gov/resource/g77e-6bhs.json` (Socrata, **no key**) | 56,087 active | 95.9% | 100% |
| CA CSLB | free CSV incl. a *personnel* file — **no API**, form download | — | — | — |

**6,249 WA rows** match `contractorlicensestatus='ACTIVE'` +
`specialtycode1desc in ('GENERAL','RESIDENTIAL')` + Seattle metro + owner + phone.

Fields: `businessname`, **`primaryprincipalname`** (owner, `"LAST, FIRST"` caps),
`phonenumber` (bare 10-digit), `address1`/`city`/`state`/`zip`.
Oregon: `full_name`, **`rmi_name`**, `phone_number`.

Build `_source_wa_lni_licences` in `lead_gen_v3.py` emitting the existing lead shape.
`owner_finder._wa_registry_lookup` already queries this dataset (#113) — reuse its
`_normalize_business` / `_person_from_principal` helpers rather than writing new ones.

**Known gaps to design around:** no email, no website (so the ad-signal detector needs
a domain resolved first — that becomes the only rationed call), no employee count (so
the 6-10-person ICP can't be filtered directly; the data is full of one-person
handymen), and **CA — the #1 ICP geography — has no API.**

## 8. 🔴 OWNER ACTIONS
1. **Merge #118** (or have Claude do it) — deploys the outreach gates and quarantines
   the 51 legacy auto rows.
2. **Keep `OUTREACH_AUTOPILOT=0`** until that deploy lands.
3. **Cold email is DEFERRED, deliberately.** No domain, no business email, 48 sends →
   0 replies. Fixing it needs a domain (~$12/yr `.com`; `.ai` is ~$70-110/yr) + DNS +
   2-3 weeks warmup. The fail-closed gate in #118 enforces the deferral automatically.
   **The phone channel needs none of this and is fully built.**
4. **OROVA Agency Facebook page: 2 followers, no website, unverified.** Prospects will
   look it up mid-call.
5. `CA_SOS_API_KEY` (CALICO) still pending government approval.

## 9. ❓ UNANSWERED — asked twice, never answered
**Q2:** which of these root scripts does Mark run BY HAND?
`diagnose_ai.py`, `cleanup_db.py`, `sanity_check_sheets.py`, `check_auth.py`
(plus `hunt_leads.py`, `health_check.py`, `test_auth.py`, `test_free_orova.py`,
`claude-desktop-prompt.txt`, `start_nova*.bat`, `orova_worker.service`).

All 11 are unreferenced by `app/`, `scripts/`, `tests/`, Dockerfile, Procfile and CI —
but an operator script looks identical to dead code from a grep. **They were left in
place.** `pytest.ini` (in #118) stops the two misnamed test files being collected, so
they are harmless meanwhile.

## 10. ⚠️ NOT AUDITED — a real hole
The Phase 1 audit was static analysis plus targeted live verification, **not a security
review**. Still unexamined: **XSS in `mission-control/`** (lead data is scraped from the
public web and rendered in the dashboard), unauthenticated mutating endpoints, and
blocking I/O inside `async def` handlers. Two attempts at a parallel agent audit died
on session limits.

**628 tests passing is NOT "no bugs."** Roughly ten real bugs were found this session in
code that was already shipped and green.

## 11. The number that matters
**48 emails sent, 0 replies. 0 calls made. 0 meetings booked. Zero prospect
conversations, ever.**

Not one of this session's fixes moves that. What gates it is having **nobody on-ICP to
contact** — §6 and §7. The next artifact that should exist in this repo is **a
transcript**, not another ADR.

## Linked
- [[0014-licence-registries-as-the-discovery-source]] · [[audit-2026-07-26-phase1]] · [[audit-2026-07-26-phase5-final]]
- [[session-2026-07-26-merge-and-discovery-wall]] · [[session-2026-07-24-handoff]] · [[active-context]] · [[traction-playbook]]
