---
name: session-2026-07-12-handoff
description: Master paste-into-fresh-chat handoff for OROVA / Nova / HermesClaw
type: session
created: 2026-07-12
status: active
---

# OROVA / HermesClaw — Master Handoff (2026-07-12)

Paste this whole file into a new Claude Code chat to continue with full context.
**Usage limits get hit often — prefer working DIRECTLY over spawning subagents.
Every subagent spawned this session (6 total) died mid-task on the session
limit; salvage-inline is the proven pattern, not delegation.**

---

## 1. What this is & the one goal

- **OROVA** = a **luxury AUTOMOTIVE marketing agency** (owner narrowed it to
  automotive on 2026-07-12 — dealerships, exotic rentals, detailing, PPF/wrap,
  mods, restoration; West Coast US). Runs Meta (FB+IG) ads for these clients.
  *(Config still lists home builders + RE agents from PR #39 — pending owner's
  explicit "automotive-only" yes before narrowing business_context.json.)*
- **Nova** = the autonomous agent that IS the agency (`app/`, Python/FastAPI on
  Render free tier, `orova-nova.onrender.com`).
- **HermesClaw** = the umbrella system (Nova + Electron desktop GUI + the vault
  brain). The Electron GUI is NOT the revenue path.
- **THE GOAL: land the first paying client.** Everything serves this. No client,
  no revenue yet. The company is heavily over-built relative to its stage — the
  gap is not engineering, it's that **no outreach has ever been sent to a real
  prospect.**

## 2. Owner (Mark) context — critical

- **Solo founder, effectively $0 budget.** Can't currently spend even $12 on a
  domain. This is real; plan around it.
- **The $0 go-to-market truth (agreed this session):** the engine finds leads +
  owner emails for FREE (SerpAPI + Prospeo). For client #1 Mark does the SEND
  manually — **personal Gmail (verified inboxing to friends, not spam) + phone
  calls** — no domain needed. Domain/SPF/DKIM + full send automation are
  POST-revenue. Do not push spend he doesn't have.

## 3. Business model (source of truth: `app/core/business_context.json`)

- **P1 $4,000/mo** (Meta lead-gen + AI creatives). **P2 $5,000/mo** (P1 +
  Retell AI cold-call qualification + CRM + booking).
- Terms: 1-month trial → 1/3/6 months (6mo = best per-month: $3k/$4k). Pay via
  Wise/ACH. **Ad spend is client-paid, direct to Meta — never through OROVA.**
- **No full refunds** (build/setup non-refundable; unused retainer only).
- **Margin ~90%+** realistic (75-80% floor for external convos).

## 4. Owner playbook — how Mark decides (`vault/hermesclaw-orova/playbook/`)

Built from an 11-question interview this session. Any AI acting as Mark obeys:
- **Respect is mutual, non-negotiable.** Impossible asks → educate once, then
  walk. Attitude = dealbreaker even at $5k/mo.
- **Price is the price.** No discounts, no free months, ever.
- **The past is closed.** NO past-client claims/numbers/verticals in outreach —
  forward-looking + system-promise only. **NEVER mention Mark's previous
  agency** (he has real past clients, so "no, you're not my first" is honest).
- **Cadence:** 1 email + 3 spaced follow-ups + 1 call → stop forever (5 touches
  max). No fake urgency, no multi-channel bombardment.
- **Always escalate to Mark:** legal/press, cancellations, refunds, big-name
  opps, anything that could harm OROVA or a client.
- **Tie-breaker ranking:** revenue > certainty > reputation > speed — but always
  INSIDE the red lines. Unsure = don't act = escalate.
- Machine-enforced mirror lives in `business_context.json` (`case_studies`,
  `outreach.cadence_policy`).

## 5. Architecture

- `app/` — Nova. **9 worker lanes** (`app/worker.py`, `schedule` lib in a daemon
  thread from FastAPI lifespan): 1 fast/approvals (2m), 2 hunt (60m), 3
  reply+drip (5m), 4 cold-call escalation (30m), 5 Drive backup (3h), 6 CEO
  brief (daily), 7 health (2h), 8 self-improvement (6h), 9 drip sender (1h).
- **LLM routing** (`app/core/ai_client.py`): Groq `llama-3.3-70b-versatile`
  (primary) → native Gemini 2.5-flash. Tier-3 OpenRouter removed (dead key).
- **Lead engine:** SerpAPI Google-Maps discovery (`lead_gen_v3.py`) → owner-name
  resolver (`owner_finder.py`, registry-first — but registries mostly dead, SERP
  is the real free source) → owner-email finder (`email_finder.py`, Prospeo) →
  enrichment (`light_enrich.py`, one AI pass under 25s ceiling).
- **Gates:** approval (`approval_gate.py`, Telegram HITL), DNC (`dnc.py`,
  opt-out list fail-closed + national-registry scrub env-gated fail-open),
  semantic firewall, circuit breakers.
- **Learning:** Wilson-bound champion/challenger (`self_improvement.py`,
  `self_learning.py`) over strategies AND skill versions (ADR-0004 P1+P2 shipped).
  **Currently 0-sample — it can't learn until real outreach volume exists.**
- **State:** SQLite, backed up to Google Drive (WORKING as of 2026-07-11) +
  Sheets fallback for leads.
- `vault/` = Obsidian shared brain. `mission-control/` = dashboard.

## 6. What shipped THIS session (PRs #32–#58, all merged, suite 271 passing)

- **Owner-email finder** (Tomba/Prospeo/Verifalia). Prospeo's old `/email-finder`
  was DEPRECATED → rewired to `/enrich-person`, **live-verified** (iLusso →
  `todd@ilusso.com`). Prospeo free tier = 100/mo. Tomba/Verifalia skipped
  (webmail/disposable-email signup blocks) — Prospeo alone suffices.
- **Drive-backup data-loss SAGA — RESOLVED.** Every deploy was wiping prod
  SQLite. Root causes fixed in sequence: Sheets-restore row crash (#34);
  `GOOGLE_CREDENTIALS_JSON` wired into Drive (#45) but service accounts CAN'T
  upload (403); OAuth refresh-token helper (#46, `scripts/get_google_refresh_
  token.py`); trio set on Render → **first `[Vault] Uploaded` VERIFIED live
  2026-07-11**. Data loss loop closed.
- **Secret redaction** (#37) after a Telegram token leaked to logs.
- **CEO-brief funnel math** (#38) — WoW conversion vs benchmark bands,
  verified in a live brief.
- **ADR-0004 Phase 1+2** (#36/#40) — vault-context injection, skill-outcome
  tracking, improvement log, SkillChallengerEvaluator in Lane 8.
- **business_context §6** (#39) — ICP/margin/calling-policy edits + hunt rotation.
- **Security cleanup:** Dependabot criticals 4→0 (#41) + highs cleared via
  openclaw 2026.5.6 / axios / undici (#56); code-scanning 21 real+FP triaged
  (#54/#55, dismissals); secret-scanning → 0.
- **Mission-control overflow fix** (#42), dead-code removal (#51), DNC national
  scrub gate (#52), **owner-name quality gate** (#57 — rejects "Maintenance
  Manual"-type junk), **TARGET_NICHE range parsing** (#58 — a comma list now
  rotates one niche/run instead of being mashed into one query).
- **Council installed** (claude-council, hex-plugins) — Gemini seat live,
  stop-gate armed; CLAUDE.md council mandate written. It's CLAUDE's advisor.
- **Owner playbook** (11-Q interview → 6 skill files) + **close-kit** (agreement,
  invoice, onboarding in `vault/hermesclaw-orova/close-kit/`).
- **Artifacts:** mission-log page + a print-ready agreement (URLs in chat).

## 7. LIVE ENGINE TEST (2026-07-12) — the key finding

Ran a real production hunt. **Verdict: the engine RUNS end-to-end but output
is NOT client-grade yet.** It found real businesses w/ real phones+sites, BUT:
they were **auto-REPAIR/oil-change shops, not luxury** (wrong ICP); owner names
were mostly blank or junk; emails were generic (support@/info@), not owner-
direct. Root cause: `TARGET_NICHE` on Render is a broad/generic value pulling
the wrong tier. Fixes shipped: owner-name quality gate (#57) + range parsing
(#58). **Not yet re-run with clean automotive niches.**

## 8. Immediate next steps (in order)

1. **Owner confirms "OROVA = luxury automotive only?"** → then align
   `business_context.json` + `DEFAULT_HUNT_NICHES` to automotive (drop home
   builders/RE agents). *Business-logic change — needs explicit yes.*
2. **Owner sets `TARGET_NICHE` on Render** to a clean comma-list (range fix
   makes it rotate now):
   `exotic car dealer california, luxury car dealership california, exotic car
   rental california, classic car restoration california, ceramic coating shop
   california, paint protection film installer california, car wrap shop
   california, luxury auto detailing california, performance tuning shop
   california`
3. **Re-run the live hunt** (`POST /api/actions/hunt-leads`), inspect
   `/api/leads` for named owners + direct emails. Verify the fixes worked.
4. **Owner emails the best 10 from personal Gmail + calls 3** — first real
   outreach. Optionally mail-tester.com check first.
5. Only after a reply/close: domain+SPF/DKIM, autopilot flags, scale.

## 9. Owner env actions still open (Render)

- **`TARGET_NICHE`** → automotive list (above). BIGGEST lead-quality lever.
- **Rotate `TELEGRAM_BOT_TOKEN`** via BotFather — it leaked to logs/transcripts
  (real token starts `8551361156:`). Only genuinely-exposed credential.
- **Booking link** (`CALENDLY_LINK`/`CAL_COM_EVENT_SLUG` + `CAL_WEBHOOK_SECRET`)
  — HOT replies currently get a link-less booking message.
- SET & WORKING: `PROSPEO_API_KEY`, Google Drive OAuth trio, `GOOGLE_API_KEY`
  (new `AQ.`-format Gemini key), `GROQ_API_KEY`, `AGENTMAIL_API_KEY`,
  `SERPAPI_KEY`, `GOOGLE_CREDENTIALS_JSON`. DELETED: `OPENAI_API_KEY`/
  `OPENAI_BASE_URL` (the "401 User not found" source) + dead COMPOSIO/MIMO/
  SGAI/OROVA_API_KEY/CRON_SECRET.

## 10. Hard constraints (don't "fix" these)

- Render free tier: 512MB, ephemeral disk, no headless browser, SMTP blocked
  (email verify = MX only), 25s enrichment ceiling. `$0` until revenue.
- `httpx==0.27.2` pinned (starlette/mcp/ollama). 4 benign starlette warnings.
- **TCPA:** call published BUSINESS lines only, never personal cells.
- Approval-gated: cold email/calls/replies (unless `*_AUTOPILOT=1`); ads/spend/
  client-signing ALWAYS human.
- Branch-first git (never commit to main), PR + merge. Vault notes carry
  frontmatter; never edit `vault/.obsidian/`; docs never at repo root.
- Backup works now, but still batch merges and check `/api/logs` after deploys.

## 11. Working style / model strategy

- **Fable (me) working directly, one thing at a time, is the realistic model
  strategy.** Multi-model orchestration is aspirational — subagents keep dying
  on usage limits (6/6 this session). When delegating, use isolated worktrees +
  "push branch don't merge" so partial work is salvageable.
- Truth protocol: report Verified / Inferred / Unknown; never claim a
  merge/test/deploy not actually observed.
- Council (`/claude-council:ask`) available next session as Claude's advisor;
  stop-gate reviews diffs. Needs a provider key (`GEMINI_API_KEY` set locally).

## 12. Key files & commands

- Tests: `python -m pytest tests -q` (271 baseline). TS: `npx vitest run` (40).
  Typecheck: `pnpm typecheck`.
- Session start: `python scripts/vault_pull.py`; read
  `vault/hermesclaw-orova/{README,STATUS,progress,decision}.md` +
  `vault/10-brain/active-context.md`.
- Live test note: scripts must `load_dotenv(<repo>/.env, override=True)` — the
  shell has an empty `SERPAPI_KEY` shadowing `.env`.
- Prod check: `/health`, `/api/logs`, `/api/leads` with `X-API-Key: <DASHBOARD_
  API_KEY>` against `RENDER_EXTERNAL_URL`.

## 13. The honest bottom line

The machine is excellent and nearly everything is green. **The business has
never contacted a customer.** The single highest-value action is not more
code — it's confirming the automotive ICP, pointing the engine at it, and
Mark sending the first 10 emails from his Gmail today. Build discipline:
revenue first; do not advance the roadmap or add capability without a
customer or explicit approval.
