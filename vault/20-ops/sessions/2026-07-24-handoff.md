---
name: session-2026-07-24-handoff
description: Cold-start handoff — the strategy pivot (ADR-0011/12/13), 16 PRs shipped, owner actions, hard constraints, and the exact spec for the next code task
type: session
created: 2026-07-24
status: active
---

# OROVA NOVA — Handoff (2026-07-24)

## 0. Read first
`CLAUDE.md` → [[active-context]] → [[traction-playbook]] → ADRs
[[0012-icp-rerank-and-pilot-pricing]], [[0013-painkiller-positioning-and-real-competition]].
Previous handoff: [[session-2026-07-22-handoff]]. Auto-memory:
`prod-debug-workflow`, `owner-execution-style`, `groq-gemini-free-tier-limits`.

**Today was a STRATEGY day, not a features day.** The engine barely changed; what
OROVA sells, to whom, and how it opens changed completely.

## 1. Production state
- **main `f9fd206`**; verify `/health` `build` matches (HTTP 200 is NOT proof).
- Healthy all day: `db: ok`, `memory: ok`. Memory pressure much improved (jemalloc
  + ~150-250MB of deps removed).
- **~20 leads**, mostly legacy junk. **Exactly ONE clears the outreach bar:**
  West Coast Exotic Cars — **Eric Curran, +1844-555-0102, confidence 83**
  (`owner_source: linkedin_public`, cross-referenced with serpapi).
- **Nothing has ever been emailed or called. Zero prospect conversations.**

## 2. What shipped today (16 PRs, all deploy-verified)
**Memory (#98-100):** removed orphaned legacy files; dropped 5 heavy deps (spaCy,
jobspy, timezonefinder, sqlmodel, yagmail) ≈150-250MB; added **jemalloc**
(`LD_PRELOAD` + decay tuning). CI caught a latent booking-funnel bug — `dateutil`
was only present transitively via pandas; now declared.

**Lead quality (#101-104):** `outreach_ready` computed gate (name + direct email +
phone, with `blockers`); `state` persisted so the SoS registry can fire; generic-email
set widened (`leads@` was leaking as "direct"); **public-LinkedIn waterfall source**
— first via DDG (produced nothing: DDG blocks datacenter IPs), then **fixed to
SerpAPI**, which produced **the first lead ever to clear the bar** (Eric Curran,
conf 40→83 via cross-source agreement — ADR-0009 working as designed).

**Strategy (#105-108):** ICP re-rank + pilot pricing (ADR-0012) · painkiller
positioning + competitive intel (ADR-0013) · Retell outbound pilot script ·
PH/US timezone constraints. Plus ADR-0011 (AI-technique fit).

## 3. THE PIVOT — what changed today
### ICP re-ranked by deal economics (ADR-0012)
Filter: **does ONE extra closed deal/month more than cover the ~$6,500-7,500
ALL-IN cost?** ("Ad spend is separate" is an accounting fact, not a buying fact.)
1. **Custom home builders / high-end remodelers — LEAD** (a $100K job grosses
   $20-50K → one job pays 4-7 months)
2. Med spas / aesthetics
3. Luxury RE top producers only

**Exotic auto → opportunistic** (economics work; a $200K buyer doesn't start on a
Meta lead form). Hunt rotation re-weighted **auto 50%→14%, homes 21%→50%** —
note `random.choice()` is uniform, so **entry count IS the weighting**.
**Disqualify:** general auto repair (~$400/job) · franchised dealers (OEM co-op) ·
already under agency contract.

### Positioning (ADR-0013)
- **Sell the painkiller, never growth.** Growth framing loses to Angi's ~$400
  anchor (we ask 16x that).
- **Pain A "The Gap"** (crew idle, existential) → P1 or P2.
  **Pain B "The Wasted Saturday"** (weekly, the wedge) → **P2 ONLY — P1 makes it
  worse.** This **supersedes profitability-plan §2.2's "always lead with P1".**
- **Real enemy #1 = inertia** (zero switching cost, worked 15 years). Only a
  deadline he already feels beats it → hence the hard qualifiers.
- **Real enemy #2 = the Angi price anchor.** Never argue price — **change the unit**
  to cost-per-idle-week-of-payroll.
- **ONE differentiator:** every lead phoned + AI-qualified in minutes. **Never
  pitch** "we're AI-operated" / "you're talking to our AI" / "AI creatives".
- **Client #1 = a $1,500-2,000/mo 60-day pilot that buys a CASE STUDY**, not revenue.

### AI techniques evaluated (ADR-0011)
PPO/GRPO/process-reward-models: **N/A** (zero training infra; hosted APIs only).
MCTS/test-time-compute-scaling: **counterproductive** (quota-starved). RSI:
**rejected** (conflicts with human-approval + never-fabricate). Adopted as guidance
only: **in-context learning** (deferred until real outcomes), targeted CoT/best-of-N.

## 4. 🔴 OWNER ACTIONS (ranked — these unblock everything)
1. **Set a booking link** — `CAL_COM_EVENT_SLUG` or `CALENDLY_LINK`. **Currently
   UNSET.** Retell can pitch perfectly and a "yes" has nowhere to land.
   **Constrain availability to US 6-9am PT (= 9pm-midnight PH) and 4-5pm PT
   (= 7-8am PH) ONLY** — otherwise the calendar fills with 3am meetings.
2. **Paste the Retell script** (`business_context.json` → `retell_pitch`) into the
   Retell agent's prompt. **It is NOT live** — `outbound_dialer` calls by
   `RETELL_AGENT_ID`; the prompt lives in Retell's dashboard.
3. **CALICO / CA SoS API key** — account created, subscription **pending manual
   government approval** (no published SLA; expect days). When approved set
   `CA_SOS_API_KEY` → authoritative owner names. Also free: `WA_SOS_ENABLED=1`.
   *(OpenCorporates = £2,250/yr, Prospeo API = paid-only. Both dead for $0.)*
4. **Run a hunt** so remodeler leads exist; approve the first call batch manually
   (do NOT flip `CALLS_AUTOPILOT`) and **listen to every recording**.
5. `BUSINESS_POSTAL_ADDRESS` (CAN-SPAM) · rotate the leaked `TELEGRAM_BOT_TOKEN`.

*`TARGET_NICHE` is confirmed NOT set — the curated rotation is live. The generic
auto-repair rows in the pipeline are legacy restored data.*

## 5. Hard constraints
- **Mark is in the Philippines (UTC+8), 15h ahead of CA.** Dialling US numbers is
  prohibitively expensive → **Retell owns ALL cold dialling.** Do not propose
  "just call them yourself." Sales meetings run on Google Meet (free, geography-
  irrelevant). VoIP is cents/min if he ever must dial.
- **512MB Render free tier** — better since jemalloc, but keep `reenrich-leads`
  batches small (endpoint caps at 5).
- **$0 budget.** SerpAPI 250/mo is shared across discovery + owner lookup +
  the LinkedIn source (all rationed through `owner_finder:serp_monthly`).
- **Groq + Gemini free tiers 429 together**; OpenRouter key now set (tier 3).
- `httpx==0.27.2` pinned. Deploys wipe SQLite; Drive+Sheets restore repopulates.
- **Never fabricate.** No invented lead data, case studies, or portfolio work.

## 6. ▶ NEXT CODE TASK — ad-signal detector (full spec, ~40 lines + tests)
**Why:** Nova cannot query the Meta Ad Library (API is political-ads only; the UI
needs a browser Render can't run; **and scraping Meta risks the ad account the whole
business depends on**). But the *signal* is free: a **Meta Pixel in their homepage
HTML proves they run Meta ads**; an **Angi/HomeAdvisor badge proves they pay for
leads and names the competitor** — together they satisfy the ICP qualifier
"already paying for leads and unhappy."

**Implementation (all facts verified today):**
1. Pure fn `detect_ad_signals(html: str) -> dict` in `app/skills/light_enrich.py`.
   Detect in the HTML: `connect.facebook.net` / `fbevents.js` / `fbq(` →
   `meta_pixel` · `gtag(`/`googleadservices` → `google_ads` ·
   `angi`/`homeadvisor`/`thumbtack`/`houzz` links or badges → `paying_for_leads`
   (record WHICH) · `free estimate`/lead form → `marketing_mature`.
2. Call it in `enrich_lead_lite` (`light_enrich.py:880`) — **the homepage HTML is
   already fetched** by `_fetch_page` (returns `{"html","markdown"}`, lines 225-287),
   so this costs **zero extra requests**.
3. Persist: add `ad_signals TEXT DEFAULT ''` to the canonical schema in
   `app/core/_db_base.py` (leads CREATE TABLE at line ~28; `evidence_json TEXT
   DEFAULT ''` at line 50 is the pattern to copy), then thread it through
   `save_lead` in `app/core/_lead_repo.py` — column list line **132**, values line
   **142**.
4. Surface in `/api/leads` (`app/main.py` ~770, alongside `confidence` /
   `outreach_ready`).
5. Tests: pure-function fixtures (pixel present/absent, Angi badge, false positives).
6. **Do NOT change `score_lead_icp` in the same PR** — surface first, score later.

## 7. Deferred (owner-gated, don't rush)
- **Enrichment consolidation** (ADR-0010 §3) — 3 crawls/lead today
  (`enrich_lead_4step` → `enrich_lead_lite` → waterfall). **Merge, don't skip** —
  `enrich_lead_lite` owns the MX/Verifalia/`email_status` logic ADR-0008 built.
  Trace in [[session-2026-07-22-improvement-research]] Appendix.
- **AI-OS excision** (~5,000 LOC) — staged, one seam at a time.
- `firecrawl-py` removal · event-log KPI ladder · Yelp waterfall source.
- Deliverability (SPF/DKIM/DMARC, RFC-8058 one-click unsubscribe, warmup ramp) —
  build when a sending domain exists.

## 8. How to work here
Branch → small PR → wait for gates (`Analyze`×3 + `test (3.11)`; **Kilo is
advisory**) → merge → **watch `/health` for the merged SHA**. Vault-only changes are
byte-identical images (Docker copies only `app/` + `mission-control/`).
Prod debug: `GET /api/logs?limit=N` with `X-API-Key: $DASHBOARD_API_KEY`.
Knowledge gate: `python scripts/compile_knowledge.py --check` (lints
`business_context.json` prices against canonical — **never change package prices
without updating `knowledge/facts/company.json`**).
Commit trailer: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

## 9. ⚠️ The thing that matters more than any of this
**Zero emails sent. Zero calls made. Zero prospect conversations. Ever.**

Every strategic artifact produced today — the ICP ranking, the two pains, the
early-adopter profile, the competitive map, the positioning — is **well-reasoned
inference, not evidence.** Each document says so explicitly, on purpose.

`active-context` carries a **BLOCKING ACTION** block: *no further positioning or
targeting work before 20 conversations.* Honour it. The next artifact that should
exist in this repo is **a transcript**, not another ADR.

## Linked
- [[traction-playbook]] · [[0011-advanced-ai-technique-fit]] · [[0012-icp-rerank-and-pilot-pricing]] · [[0013-painkiller-positioning-and-real-competition]] · [[session-2026-07-24-strategy-pressure-test]] · [[session-2026-07-22-improvement-research]] · [[active-context]]
