# OROVA (Render-friendly) dependencies
# Keep this file lean for low-memory deploy targets.
# Heavy/optional scraping deps are moved to requirements-mega.txt.

agentmail==0.4.15
beautifulsoup4==4.12.3
duckduckgo-search==8.1.1

# Web framework (required by app/main.py)
fastapi==0.104.1

google-auth==2.29.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.126.0
google-generativeai==0.8.3
gspread==6.1.0

# 0.27.2: newest release that still works with starlette 0.27 TestClient while
# satisfying mcp (>=0.27.1) and ollama (>=0.27) installed alongside in the
# hermes-agent venv. Do not bump to 0.28 until fastapi/starlette are upgraded.
httpx==0.27.2
nest-asyncio==1.6.0
openai==1.51.0
# redis==5.0.8  # REMOVED: app falls back to SQLite automatically — saves ~30MB RAM

# python-telegram-bot==20.8  # REMOVED: never imported — all Telegram traffic
#   uses the raw Bot HTTP API via httpx. Its httpx~=0.26 pin also conflicted
#   with httpx==0.27.2 and broke clean builds on Render.
python-dotenv==1.2.2
python-multipart==0.0.31
pydantic==2.13.0
pydantic-settings==2.14.2
pytz==2024.1

# reportlab==4.2.0  # REMOVED: not used anywhere in app/ — saves ~15MB RAM
requests==2.33.0
retell-sdk==4.1.0
schedule==1.2.1
tenacity==8.3.0
apscheduler==3.10.4

# Optional server tooling (not required for the built-in HTTPServer, but kept for dev)
uvicorn==0.29.0
# yagmail==0.15.293  # REMOVED 2026-07-22: zero imports in app/ (AgentMail is the
#   mailer) — dead weight. Restore from requirements-mega.txt if ever needed.

# [NEW] Email & Phone Validation (Render-friendly, lightweight)
email-validator==2.2.0
phonenumbers==8.13.0

# [NEW] DNS MX verification for email guess validation (free, no API key)
dnspython==2.6.1

# spacy==3.8.14  # REMOVED 2026-07-22: was a 6th-fallback owner-name NER
#   (~100-150MB RAM + spacy/thinc/blis/numpy image tree). Superseded by the
#   ADR-0009 decision-maker waterfall (registry + email-localpart + recognized
#   names); low-precision "any PERSON entity" extraction is the anti-pattern
#   ADR-0008/0009 moved away from. light_enrich already degrades via
#   `except ImportError` when the model is absent. Biggest single RAM reclaim.

# [NEW] Email Templating & Lead Scoring (Free ML)
jinja2==3.1.6
# scikit-learn==1.3.2  # REMOVED: 80MB+ RAM, replace with rule-based scoring

# python-jobspy==1.1.82  # REMOVED 2026-07-22: pulls pandas+numpy (heavy) for a
#   job-postings scanner that never runs on the live SDR lanes (only reachable
#   via the bypassed AI-OS planner). job_signal_hunter guards the import now.
# sqlmodel==0.0.14  # REMOVED 2026-07-22: zero imports (app/models/core.py was
#   deleted); dragged in the SQLAlchemy tree. DB layer uses raw sqlite3.
# timezonefinder==6.2.0  # REMOVED 2026-07-22: ~40-50MB polygon data used ONLY by
#   the dead get_timezone_from_coords path; the pipeline uses the lightweight
#   get_timezone_from_state lookup (US-only ICP). Coords path removed.
firecrawl-py==1.1.0  # TODO: removable — key-gated ($0, never keyed); pending a
#   verify of lead_finder's except breadth. Deferred to a follow-up PR.
# scrapling REMOVED 2026-07-13: never imported in shipped source (only stale
# .pyc of the deleted scrapling_scraper experiment referenced it). It was
# unpinned, so its 2026-07-12 0.4.11 release + heavy native subtree (lxml,
# curl_cffi, w3lib) landed in every Render build for zero runtime benefit.

psutil==5.9.8
